# Especificación

Preparar el paquete `gi-common-tenants` para la versión correctiva `0.1.2`.
`pyproject.toml`, `gi_common_tenants.__version__`, el wheel y la
documentación deben coincidir exactamente. Se conservan las APIs públicas y
no se modifica ni sobrescribe la release `v0.1.1`.

Fuera de alcance: Core, Persons, CRM, otros repositorios, tags, releases y
merge automático.
