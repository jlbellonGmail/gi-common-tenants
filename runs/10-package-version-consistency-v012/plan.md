# Plan

1. Cambiar metadata y versión pública a `0.1.2`, actualizando la prueba.
2. Construir un wheel limpio y verificar metadata, import y APIs públicas.
3. Actualizar documentación y registrar la decisión de no tocar `v0.1.1`.
4. Ejecutar suites, PostgreSQL/RLS disponible en CI, contratos, integridad y
   revisión independiente; dejar la unidad en `READY_FOR_PR`.
