# Decisión

Se adopta configuración declarativa por repositorio en lugar de una lista
fija de fases. La fuente de unidades exigibles es el ROADMAP real, excluyendo
únicamente contenido de ejemplo dentro de fenced code blocks. No se agrega
una lista de exclusión por nombres ni se relajan gates de seguridad.
