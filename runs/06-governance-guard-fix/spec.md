# Especificación

- Corregir la invocación inválida de `gh api` en el fallback del guard.
- Mantener los controles de PR fusionada, actor, concurrencia y remediación.
- Añadir una regresión que impida volver a pasar `--arg` a `gh api`.
- Documentar la causa raíz y ejecutar CI completo.
