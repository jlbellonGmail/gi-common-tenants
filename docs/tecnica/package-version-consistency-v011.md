# Package version consistency v0.1.1

The public module version is aligned with the `0.1.1` distribution metadata.
The existing tag and release remain immutable; this PR provides the corrected
source and a reproducible wheel without creating a new tag or release.
