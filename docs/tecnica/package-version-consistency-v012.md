# Consistencia del paquete v0.1.2

La corrección prepara `gi-common-tenants` para `0.1.2`: metadata de
distribución, `gi_common_tenants.__version__`, wheel y documentación deben
coincidir. La release histórica `v0.1.1` no se sobrescribe.
