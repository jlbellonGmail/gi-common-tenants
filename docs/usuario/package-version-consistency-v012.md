# Versión del paquete v0.1.2

La versión preparada del paquete es `0.1.2`. El wheel se verifica en un
entorno virtual limpio antes de la PR. La publicación requiere una decisión
humana de release y no altera `v0.1.1`.
