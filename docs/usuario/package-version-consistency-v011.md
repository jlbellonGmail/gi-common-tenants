# Versión del paquete v0.1.1

La fuente corregida y el wheel reproducible exponen `0.1.1` tanto en los
metadatos de distribución como en `gi_common_tenants.__version__`. El asset
publicado existente se conserva sin sobrescribir; la corrección queda en PR.
